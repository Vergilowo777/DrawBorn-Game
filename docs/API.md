# API

Phase 0 exposes only:

- `GET /health` → `{ "status": "ok", "phase": 0 }`

Authentication, database persistence, battle, AI, arena, purchase, and UGC endpoints are
deferred to their designated phases.