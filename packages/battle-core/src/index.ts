export interface BattleCoreCompatibility {
  readonly deterministic: true;
  readonly runtimeDependencies: 0;
}

export const BATTLE_CORE_COMPATIBILITY: BattleCoreCompatibility = {
  deterministic: true,
  runtimeDependencies: 0
};
